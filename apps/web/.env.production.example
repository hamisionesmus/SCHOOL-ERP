# Copy this to `.env.production.local` inside apps/web on the server (once, by hand — never
# committed). NEXT_PUBLIC_API_URL is baked into the client bundle at BUILD time, so it must exist
# before `npm run build` runs, not just at start time.
NEXT_PUBLIC_API_URL=https://api.myschoolapp.xyz
